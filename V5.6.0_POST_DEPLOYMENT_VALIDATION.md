# NurseLink v5.6.0 Post-Deployment Validation

Date: 2026-08-29

Candidate: `v5.6.0-rc.1` / `9379883`

Scope: read-only checks against the established NurseLink live paths. No migrations, data writes, SLA evaluation, bulk triage, or other production-changing actions were performed.

## Result

**BLOCKED — DEPLOYED RELEASE MISMATCH**

The application and API are reachable and healthy, but the checked paths do not serve the v5.6.0 release candidate.

## Evidence

| Check | Observed result |
|---|---|
| `GET https://api.amsertech.com/api/health` | HTTP 200; build `NL-013-ultahost-pilot` |
| `GET https://api.amsertech.com/api/health/live` | HTTP 200; release `1.0.0-ultahost-pilot.1` |
| `GET https://api.amsertech.com/api/health/ready` | HTTP 200; environment `ultahost-pilot`; database, cache, private storage, queue, and configuration all reported `ok` |
| `GET https://app.amsertech.com/admin/` | HTTP 200; prior administrator markup; loads `dashboard.js?nlv=640` |
| Deployed administrator HTML markers | `applicationSlaSection`, `applicationBulkTriage`, and `applicationSavedViews` absent |
| Deployed `dashboard.js?nlv=640` markers | `saved-views`, `sla-policy`, `bulk-triage`, and `export-history` absent |

## Required corrective milestone

1. Confirm the intended v5.6.0 staging hostname and deployment root.
2. Deploy the complete `v5.6.0-rc.1` frontend and API payload from commit `9379883` to that staging target.
3. Apply migrations `045000`, `046000`, and `047000` only after a verified staging backup exists.
4. Re-run the public health and asset-marker checks.
5. Continue with authenticated administrator UAT, rollback rehearsal, and recovery-baseline creation.

Run the deterministic read-only check after each corrective deployment:

```powershell
node production-audit/verify-v560-deployment.mjs https://staging-app.example.com https://staging-api.example.com
```

The final `v5.6.0` production tag must not be created until these gates pass.
